# 🚀 БЫСТРЫЙ СТАРТ - Новая система

## ✅ Что нового:

1. ✅ **Приоритет БД** - сначала проверяем БД, потом парсим
2. ✅ **Фоновое обновление** - не блокирует ответ пользователю
3. ✅ **Словарики специальностей** - для точного поиска
4. ✅ **Семантический поиск** - маппинг между сайтами
5. ✅ **Парсинг одного источника** - для точности

---

## 🎯 Запуск системы (3 команды):

```bash
# 1. Применить миграцию (добавляет ProfessionDictionary)
npm run db:migrate

# 2. Сгенерировать Prisma клиент
npm run db:generate

# 3. Запустить API
npm run dev:api
```

---

## 📖 Обновить словарики (первый раз):

```bash
# Вариант 1: Через скрипт
npm run dict:update

# Вариант 2: Через API (после запуска сервера)
curl -X POST http://localhost:3000/api/dictionaries/update
```

**Что происходит:**
- Парсит rabota.md → ~50 специальностей
- Парсит 999.md → ~30 специальностей
- Берет makler.md → ~150 специальностей (из кода)
- Сохраняет в БД

---

## 🧪 Тестирование

### Тест 1: Первый запрос (БД пуста)
```bash
curl "http://localhost:3000/api/vacancies?keywords=developer"
```
**Ожидается:**
- `source: "fresh"` (парсинг СЕЙЧАС)
- ⏱️ ~5 секунд
- Данные сохранены в БД

### Тест 2: Повторный запрос
```bash
curl "http://localhost:3000/api/vacancies?keywords=developer"
```
**Ожидается:**
- `source: "cache"` (из БД)
- ⏱️ ~100мс
- `updating: false` (данные свежие)

### Тест 3: Парсинг одного источника
```bash
curl -X POST http://localhost:3000/api/vacancies/force-parse \
  -H "Content-Type: application/json" \
  -d '{"sources": ["rabota.md"], "searchQuery": "developer"}'
```
**Ожидается:**
- Парсится только rabota.md
- С поисковым запросом "developer"

### Тест 4: Получить словарики
```bash
curl http://localhost:3000/api/dictionaries
```
**Ожидается:**
```json
{
  "success": true,
  "data": {
    "rabota.md": [...],
    "999.md": [...],
    "makler.md": [...]
  }
}
```

### Тест 5: Семантический поиск
```bash
curl "http://localhost:3000/api/dictionaries/search?query=разработчик"
```
**Ожидается:**
- Находит похожие специальности в каждом источнике
- Возвращает similarity score (0-1)

---

## 📋 Новые эндпоинты

### Словарики
```bash
GET  /api/dictionaries                    # Все словарики
GET  /api/dictionaries?source=rabota.md   # Один источник
GET  /api/dictionaries/search?query=X     # Семантический поиск
GET  /api/dictionaries/stats              # Статистика
POST /api/dictionaries/update             # Обновить словарики
POST /api/dictionaries/update {"source":"rabota.md"} # Один источник
DELETE /api/dictionaries {"source":"X"}   # Очистить
```

### Вакансии (обновлено)
```bash
POST /api/vacancies/force-parse
{
  "sources": ["rabota.md"],      # Один или несколько
  "searchQuery": "developer"     # Поисковый запрос
}
```

---

## 🎯 Логи - что должно быть

### Первый запрос (БД пуста):
```
🔍 Поиск вакансий: { keywords: ['developer'], sources: [...] }
📊 Найдено в БД: 0 вакансий
📊 История парсинга для "developer":
   rabota.md: ❌ устарел (никогда)
   999.md: ❌ устарел (никогда)
   makler.md: ❌ устарел (никогда)

📭 Данных нет в БД, запускаю синхронный парсинг
   Источники: rabota.md, 999.md, makler.md

🚀 Запуск парсинга: ...
   ✅ rabota.md: 100 новых, 0 обновлено
   ✅ 999.md: 50 новых, 0 обновлено
   
✅ Парсинг завершен: 150 вакансий за 5000мс
```

### Повторный запрос (данные свежие):
```
🔍 Поиск вакансий: { keywords: ['developer'], ... }
📊 Найдено в БД: 150 вакансий
✅ Данные найдены в БД, возвращаю 150 вакансий
📊 История парсинга для "developer":
   rabota.md: ✅ недавно (07.01.2025, 19:00:00)
   999.md: ✅ недавно (07.01.2025, 19:00:00)
```

### Данные есть, но устарели:
```
🔍 Поиск вакансий: { keywords: ['developer'], ... }
📊 Найдено в БД: 150 вакансий
✅ Данные найдены в БД, возвращаю 150 вакансий
📊 История парсинга для "developer":
   rabota.md: ❌ устарел (06.01.2025, 06:00:00)
   999.md: ❌ устарел (06.01.2025, 06:00:00)
   
⏰ Запускаю фоновое обновление для: rabota.md, 999.md
   📋 Задача фонового парсинга добавлена: rabota.md
   📋 Задача фонового парсинга добавлена: 999.md
```

---

## 🎉 Итого

**Система готова к работе!**

- ✅ Приоритет БД → быстрые ответы
- ✅ Фоновое обновление → без блокировок
- ✅ Словарики → точный поиск
- ✅ Один источник → гибкость
- ✅ API готов → интеграция с ботом

**Для бота:**
1. Получить словарики → показать пользователю
2. Пользователь выбирает → парсинг с точным ID
3. Или пользователь вводит текст → семантический поиск

**Документация:**
- `NEW_LOGIC_GUIDE.md` - полное описание
- `QUICK_START.md` - этот файл
