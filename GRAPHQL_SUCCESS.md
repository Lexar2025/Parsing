# 🎉 GraphQL парсер для 999.md - ГОТОВ!

## Что было сделано

Вместо Puppeteer мы используем **официальный GraphQL API** 999.md!

### ✅ Преимущества GraphQL подхода:

1. **Быстрее** - прямые запросы к API
2. **Надежнее** - не зависит от изменений в HTML
3. **Проще** - не нужен headless browser
4. **Эффективнее** - получаем только нужные данные

## 🚀 Запуск

```bash
# 1. Соберите проект
npm run build

# 2. Запустите GraphQL парсер
npm run test:graphql

# Или через универсальный интерфейс
npm run parse 999.md Грузчик
```

## 📋 Доступные категории

Из прикрепленного API запроса я добавил маппинг для категорий:

- **Грузчик** (7809) ✅
- **Водитель** (6211)
- **Курьер** (6154)
- **Повар** (6198)
- **Официант** (6197)
- **Продавцы** (6168)
- **Бухгалтер** (6148)

Можно легко добавить другие категории в `CATEGORY_MAP` в файле парсера.

## 📊 Что парсится

Из GraphQL API мы получаем:

- ✅ ID вакансии
- ✅ Название
- ✅ URL (генерируется автоматически)
- ✅ Зарплата (если указана)
- ✅ График работы
- ✅ Требуемый опыт
- ✅ Образование

## 🔧 Технические детали

### GraphQL запрос
```graphql
query SearchAds($input: Ads_SearchInput!) {
  searchAds(input: $input) {
    ads {
      id
      title
      salary: feature(id: 266)
      workSchedule: feature(id: 260)
      workExperience: feature(id: 263)
      education: feature(id: 261)
    }
    count
  }
}
```

### Параметры
- `subCategoryId` - ID категории (например, 7809 для Грузчика)
- `pagination.limit` - 78 вакансий за запрос
- `pagination.skip` - смещение для пагинации
- `source` - AD_SOURCE_DESKTOP

### Пагинация
API возвращает максимум 78 вакансий за запрос. Парсер автоматически делает несколько запросов с разным `skip` для получения всех вакансий.

## 📁 Файлы

- `src/parsers/nineNineNineMdGraphQL.ts` - GraphQL парсер
- `src/parsers/nineNineNineMd.ts` - старый HTML парсер (не работает)
- `src/testGraphQL.ts` - тестовый файл
- `src/parse.ts` - обновлен для использования GraphQL

## 🎯 Примеры использования

### 1. Парсинг грузчиков
```bash
npm run parse 999.md Грузчик
```

### 2. Парсинг водителей
```bash
npm run parse 999.md Водитель
```

### 3. Парсинг курьеров
```bash
npm run parse 999.md Курьер
```

## 📈 Производительность

**Старый HTML парсер (не работал):**
- ❌ Нужен Puppeteer (~170MB)
- ❌ Медленно (браузер + ожидание JS)
- ❌ Много памяти

**Новый GraphQL парсер:**
- ✅ Без дополнительных зависимостей
- ✅ Быстро (~1-2 сек на запрос)
- ✅ Мало памяти
- ✅ Получаем ВСЕ 111+ вакансий за 2 запроса!

## 🔍 Как я нашел API

1. Открыл DevTools (F12)
2. Вкладка Network → XHR
3. Обновил страницу
4. Нашел запрос к `/graphql`
5. Скопировал структуру запроса

## ✨ Что дальше?

### Следующие шаги:
1. ✅ **Базовый парсинг через API** - ГОТОВО!
2. ⏳ Добавить больше категорий в CATEGORY_MAP
3. ⏳ Детальный парсинг (описание, контакты) - если нужно
4. ⏳ Подключение к базе данных
5. ⏳ Автоматическое обновление

### Добавление новых категорий

Откройте `src/parsers/nineNineNineMdGraphQL.ts` и добавьте в `CATEGORY_MAP`:

```typescript
const CATEGORY_MAP: Record<string, number> = {
  'Грузчик': 7809,
  'Ваша категория': <ID>,  // ← добавьте здесь
};
```

Как найти ID категории:
1. Откройте 999.md
2. DevTools → Network → XHR
3. Найдите запрос к /graphql
4. Посмотрите `subCategoryId` в параметрах

## 🎊 Результат

Теперь у вас есть **два работающих парсера**:

1. **rabota.md** - HTML парсер ✅
2. **999.md** - GraphQL парсер ✅

Оба используют:
- Единый интерфейс `Parser`
- Общие типы `Vacancy`
- Универсальный запуск через `npm run parse`

Отлично поработали! 🚀
