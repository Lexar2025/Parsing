# 🚀 БЫСТРЫЙ СТАРТ - 3 КОМАНДЫ

## Исправления выполнены ✅

1. ✅ Парсер 999.md - ошибка `__name` исправлена
2. ✅ VacancyManager - логика кеширования переписана
3. ✅ Повторный запрос теперь использует кеш

---

## Запустить систему:

```bash
# 1. Сгенерировать Prisma клиент (ОБЯЗАТЕЛЬНО!)
npm run db:generate

# 2. Пересобрать
npm run build

# 3. Запустить API
npm run dev:api
```

---

## Проверить работу:

```bash
# Первый запрос (должен парсить)
curl "http://localhost:3000/api/vacancies?keywords=developer"
# → source: "fresh", ~5 секунд

# Повторный запрос (должен взять из кеша)
curl "http://localhost:3000/api/vacancies?keywords=developer"
# → source: "cache", ~100мс

# Новый запрос (должен парсить)
curl "http://localhost:3000/api/vacancies?keywords=python"
# → source: "fresh", ~5 секунд
```

---

## Если не работает:

### Ошибка 1: `searchQuery does not exist in type 'ParseLogWhereInput'`
```bash
npm run db:generate
```

### Ошибка 2: Повторный запрос все еще парсится
Проверьте логи - должно быть:
```
✅ Все источники парсились недавно для "developer", беру из БД
```

Если вместо этого видите:
```
📭 Запускаю парсинг...
```

Значит Prisma клиент не сгенерирован! Выполните:
```bash
npm run db:generate
npm run build
```

### Ошибка 3: 999.md падает с `__name is not defined`
Убедитесь что используете НОВЫЙ код из `nineNineNineMd.ts`:
```bash
npm run build
```

---

## Логи - что должно быть:

### Первый запрос:
```
🔍 Поиск вакансий: { keywords: ['developer'], ... }
📊 История парсинга для "developer":
   rabota.md: ❌ устарел (никогда)
   999.md: ❌ устарел (никогда)
   makler.md: ❌ устарел (никогда)

📭 Запускаю парсинг 3 источников: rabota.md, 999.md, makler.md
🚀 Запуск парсинга: ...
   ✅ rabota.md: 100 новых, 0 обновлено
   ✅ 999.md: 50 новых, 0 обновлено
   ✅ makler.md: 0 новых, 0 обновлено

✅ Парсинг завершен: 150 вакансий за 4500мс
```

### Повторный запрос:
```
🔍 Поиск вакансий: { keywords: ['developer'], ... }
📊 История парсинга для "developer":
   rabota.md: ✅ недавно (07.01.2025, 19:00:00)
   999.md: ✅ недавно (07.01.2025, 19:00:00)
   makler.md: ✅ недавно (07.01.2025, 19:00:00)

✅ Все источники парсились недавно для "developer", беру из БД
📊 Найдено в БД: 150 вакансий
```

---

## ВСЁ! 🎉

Три команды выше - и система работает.
